# Core — required
requests>=2.28
rich>=13.0
flask>=3.0
werkzeug>=3.0
feedparser>=6.0
beautifulsoup4>=4.11
fpdf>=1.7
qrcode>=7.3
Pillow>=9.0
matplotlib>=3.5

# Optional — enables smarter offline learning mode (TF-IDF matching instead
# of plain keyword matching)
scikit-learn>=1.3

# Optional — enables the AI file organiser's live file-watching
watchdog>=3.0

# Optional — enables music downloads
yt-dlp>=2023.7.6
